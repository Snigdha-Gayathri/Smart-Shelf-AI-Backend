# ── Python ──
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pkl
*.h5
*.pt
.env
.env.*
!.env.example
!.env.*.example
*.egg-info/

# ── Node / Frontend ──
node_modules/
frontend/dist/

# ── Heavy assets (PDFs, large images, video) ──
# Now stored in proper locations and tracked in git
# backend/static/books/ — downloadable PDFs (tracked)
# frontend/public/images/ — UI images (tracked)
# frontend/public/videos/ — video backgrounds (tracked)

# Still ignore any stray large binaries outside the designated asset folders:
frontend/public/assets/*.pdf
frontend/public/assets/*.mp4

# ── Caches & build artifacts ──
backend/book_embeddings_cache.json
backend/quantum_similarity_cache.json
*.db
*.log

# ── Secrets / encrypted vault artifacts ──
*.env.enc
*.secrets.enc
secrets/

# ── OS / Editor ──
.DS_Store
Thumbs.db
.vscode/
.cache/
