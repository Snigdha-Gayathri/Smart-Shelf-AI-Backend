# Backend private secrets (never commit real values)
# Copy this file to .env and fill values locally or in deployment secret manager.

SKIP_ML=0
JWT_SECRET=replace_with_a_long_random_secret
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here

# Optional service keys
OPENAI_API_KEY=
MONGODB_URI=
